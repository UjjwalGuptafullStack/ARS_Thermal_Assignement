# Lecture slide decks — course material, not our work, not ours to redistribute.
# Kept locally in 07_reference/Lectures/ for reference.
07_reference/Lectures/

# Landsat v2 rasters — ~30 MB each, 139 MB total. Regenerable by running
# 02_scripts/M_landsat_daynight.js and starting the export tasks. The CSVs
# they accompany ARE tracked, so every number in the report is in the repo.
04_M_landsat/v2_rasters/

# LaTeX build artefacts
*.aux
*.log
*.out
*.toc
*.synctex.gz
*.fls
*.fdb_latexmk
*.bbl
*.blg

# Generated preview — rebuild from the .tex
06_report/UHI_report_PREVIEW.pdf

# OS and editor
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# Python
__pycache__/
*.pyc
